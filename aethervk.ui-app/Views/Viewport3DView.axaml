<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:AetherVk.Logic.ViewModels"
             xmlns:input="using:AetherVk.Input"
             xmlns:i="clr-namespace:Avalonia.Xaml.Interactivity;assembly=Avalonia.Xaml.Interactivity"
             xmlns:ia="clr-namespace:Avalonia.Xaml.Interactions.Core;assembly=Avalonia.Xaml.Interactions"
             x:DataType="vm:Viewport3DViewModel"
             x:Class="AetherVk.Views.Viewport3DView"
             input:ActionContext.Id="Viewport"
             input:ActionContext.Handler="{Binding}"
             xmlns:conv="using:AetherVk.Converters"
             xmlns:controls="clr-namespace:AetherVk.Controls">

  <Grid>
    <!-- 8px border for UI overlays to prevent airspace issues with NativeControlHost -->
    <Border Padding="8">
      <Grid>
        <!-- Placeholder solid background, to be replaced by Vulkan rendering -->
        <Border Background="#1A1A1A" />
        <controls:VulkanViewportControl x:Name="ViewportHost" DataContext="{Binding VulkanViewModel}" />
      </Grid>
    </Border>

    <!-- Removed Initialization Popup and Measurement Flyouts -->
  </Grid>
</UserControl>