<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:AetherVk.Logic.Models"
             xmlns:controls="using:AetherVk.Controls"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="600"
             x:Class="AetherVk.Views.Components.ParticleEmitterCirclesView"
             x:DataType="models:ParticleEmitterCirclesComponent">
    <Design.DataContext>
        <models:ParticleEmitterCirclesComponent />
    </Design.DataContext>

    <Expander Header="{Binding Name}" IsExpanded="True" HorizontalAlignment="Stretch" Margin="0,0,0,8">
        <StackPanel Spacing="8" Margin="4">

            <!-- List of emission circles -->
            <ListBox ItemsSource="{Binding Circles}" x:Name="circlesList"
                     Classes="list-tray" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="models:EmissionCircleItem">
                        <Border Classes="list-tray-item dense" Background="{DynamicResource Bg.Surface}"
                                CornerRadius="8" Margin="0,0,0,8" Padding="16,12"
                                BoxShadow="0 2 6 0 #1A000000">
                            <StackPanel Spacing="12">
                                <!-- Row 0: colour swatch + label + remove button -->
                                <Grid ColumnDefinitions="Auto,*,Auto">
                                    <Border Grid.Column="0" Width="18" Height="18"
                                            CornerRadius="9" Margin="0,0,12,0"
                                            VerticalAlignment="Center">
                                        <Border.Background>
                                            <SolidColorBrush Color="#FFFF9944"/>
                                        </Border.Background>
                                    </Border>
                                    <TextBlock Grid.Column="1" Text="Emission Jet"
                                               FontWeight="SemiBold" FontSize="14"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="2" Content="✕"
                                            Classes="icon" Padding="8,4" FontSize="12"
                                            VerticalAlignment="Center" Background="Transparent"
                                            Command="{Binding $parent[ListBox].((models:ParticleEmitterCirclesComponent)DataContext).RemoveCircleCommand}"
                                            CommandParameter="{Binding}"
                                            ToolTip.Tip="Remove this emission jet"/>
                                </Grid>

                                <!-- Properties wrapped for responsiveness -->
                                <!-- Fluent 3 styling uses more breathing room and stacked labels for compact spaces -->
                                <WrapPanel Margin="0,4,0,0">
                                    
                                    <!-- Position -->
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Latitude (°)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding LatitudeDeg}" HasBounds="True" MinBound="-90" MaxBound="90" Step="1" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Longitude (°)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding LongitudeDeg}" IsWrapped="True" WrapMin="0" WrapMax="360" Step="1" Width="120"/>
                                    </StackPanel>

                                    <!-- Geometry & Mass -->
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Mass (g)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding MassGrams}" HasBounds="True" MinBound="0" Step="0.1" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Radius (frac)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding CircleRadius}" HasBounds="True" MinBound="0.001" MaxBound="1.0" Step="0.01" Width="120"/>
                                    </StackPanel>

                                    <!-- Emission Params -->
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Rate (/tick)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding ParticlesPerTickDouble}" HasBounds="True" MinBound="0" Step="10" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="TTL (ticks)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding TTLDouble}" HasBounds="True" MinBound="1" Step="10" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Velocity (km/s)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding MeanVelocityKms}" HasBounds="True" MinBound="0.0" Step="0.1" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="Dir StdDev (°)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding VelocityDirStdDevDeg}" HasBounds="True" MinBound="0.0" MaxBound="180.0" Step="1" Width="120"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="β (rad.pres.)" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding Beta}" HasBounds="True" MinBound="0.0" Step="0.1" Width="120"/>
                                    </StackPanel>

                                    <!-- Colour -->
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="R" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding ColorR}" HasBounds="True" MinBound="0" MaxBound="1" Step="0.01" Width="80"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,16,12">
                                        <TextBlock Text="G" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding ColorG}" HasBounds="True" MinBound="0" MaxBound="1" Step="0.01" Width="80"/>
                                    </StackPanel>
                                    <StackPanel Margin="0,0,0,12">
                                        <TextBlock Text="B" FontSize="12" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,0,4"/>
                                        <controls:UnboundedSlider Value="{Binding ColorB}" HasBounds="True" MinBound="0" MaxBound="1" Step="0.01" Width="80"/>
                                    </StackPanel>
                                </WrapPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Add circle button -->
            <Button Content="＋ Add Emission Jet"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Center"
                    Classes="accent" Margin="0,4,0,0"
                    Command="{Binding AddCircleCommand}"/>
        </StackPanel>
    </Expander>
</UserControl>
