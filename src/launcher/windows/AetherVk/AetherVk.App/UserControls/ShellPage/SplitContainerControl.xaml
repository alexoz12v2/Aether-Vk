<UserControl
    x:Class="AetherVk.UserControls.SplitContainerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AetherVk.UserControls"
    xmlns:pages="using:AetherVk.Pages"
    xmlns:vm="using:AetherVk.Core.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cu="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <UserControl.Resources>
        <vm:SplitContainerControlViewModel x:Key="ViewModel" />
    </UserControl.Resources>

    <Grid x:Name="Container">
        <!-- Templated controls instanced dynamically need a prepared template in the control they need to be
              instantiated it-->
        <Grid.Resources>
            <cu:GridSplitter x:Key="TemplateSplitter" ResizeBehavior="BasedOnAlignment" />
        </Grid.Resources>
    </Grid>
</UserControl>
