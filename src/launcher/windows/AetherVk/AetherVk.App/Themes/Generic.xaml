<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:ctrl="using:AetherVk.TemplatedControls">

    <Style TargetType="ctrl:NumericSlider">
        <Setter Property="Template">
            <Setter.Value>
                <!-- https://learn.microsoft.com/en-us/windows/apps/develop/platform/xaml/xaml-theme-resources -->
                <ControlTemplate TargetType="ctrl:NumericSlider">
                    <StackPanel Orientation="Vertical">
                        <!-- Numeric Value -->
                        <NumberBox Grid.Row="0"
                                   Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                   Minimum="{TemplateBinding Minimum}"
                                   Maximum="{TemplateBinding Maximum}"
                                   SpinButtonPlacementMode="Hidden"
                                   Width="64"
                                   Padding="0"
                                   Background="Transparent"
                                   BorderThickness="0"
                                   FontSize="16"
                                   HorizontalAlignment="Center"
                                   HorizontalContentAlignment="Center"
                                   VerticalContentAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondary}" />
                        <!-- Slider -->
                        <Slider Grid.Row="1"
                                Minimum="{TemplateBinding Minimum}"
                                Maximum="{TemplateBinding Maximum}"
                                Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                HorizontalAlignment="Stretch" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
